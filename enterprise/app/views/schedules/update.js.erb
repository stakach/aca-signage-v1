
<% if flash[:notice] %>
	$('#calendar').fullCalendar('refetchEvents');
	$('#dialog').dialog("close");
	
	$.noticeAdd({ text: "<%= escape_javascript(flash[:notice]) %>", stayTime: 6000 });
<% else %>
	$.noticeAdd({ text: "<%= escape_javascript(render('shared/error_messages', :target => @schedule)) %>", stay: true });
<% end %>
