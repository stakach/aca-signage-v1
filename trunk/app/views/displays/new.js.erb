
$('#dialog').html('<%= escape_javascript(render('form'))%>');
$('#dialog').dialog({
	modal: true,
	title: '<%= "#{t(:new)} #{t(:display)}" %>',
	buttons: {
		'<%= t(:add) %>': function() {
			$.ajax({
				type: "POST",
				dataType: 'script',
				url: $('#dialog form').attr('action'),
				data: $('#dialog form').serialize(),
				error: function(xhr, textStatus, errorThrown) {
					error_notice(xhr, '<%= "#{t(:unable_to_create)} #{t(:display).downcase}" %>', errorThrown);
				}
			});
		}
	}
});

